// Widget tests are disabled while the app requires Supabase initialization.
// Integration tests should be used to test authenticated flows.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder test — app requires Supabase init', (WidgetTester tester) async {
    // TODO: adicionar testes de integração com mock do Supabase.
    expect(true, isTrue);
  });
}
